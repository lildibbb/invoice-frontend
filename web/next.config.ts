import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler temporarily disabled due to @babel/types@7.29.0 incompatibility
  // reactCompiler: true,
};

export default nextConfig;
